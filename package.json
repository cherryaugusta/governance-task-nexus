{
  "name": "governance-task-nexus",
  "version": "1.2.0",
  "description": "Lightweight frontend-first operational governance task control plane",
  "type": "module",
  "scripts": {
    "dev": "npx serve . --listen 3000",
    "preview": "npx serve . --listen 3000",
    "lint": "eslint js/",
    "lint:fix": "eslint js/ --fix",
    "format": "npx prettier --write .",
    "format:check": "npx prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.3.0"
  }
}
